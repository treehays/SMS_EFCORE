namespace SMS;
class Program
{
    static void Main(string[] args)
    {
        var mainMenu = new MainMenu();
        mainMenu.AllMainMenu();
        // Students students = new Students();
        // Console.Write("FUll Name: ");
        // students.FullName = Console.ReadLine();
        // Console.Write("Department: ");
        // students.Department = Console.ReadLine();
        // Console.Write("DOB (YYYY/MM/DD): ");
        // string dobst = Console.ReadLine();
        // DateTime dobs = (DateTime.Parse(dobst));
        // students.DateOfBirth = dobs;
        // students.PrintStudentsDetails(students);
        // Console.WriteLine("Hello, World!");
    }
}

//dotnet ef database update